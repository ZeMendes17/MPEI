%% c)
% sendo A a probabilidade de a soma de ambos os lançamentos ser 9
% isso so acontece em 3 casos {(3,6); (4,5); (6,3)}
% uma vez que os casos possiveis são 6^2, P(A)=1/9;

% sendo B a probabilidade de o segundo valor ser par, os casos favoraveis
% sao 3 e os possiveis 6, logo P(B)=1/2;

%para saber se os acontecimentos são independentes é necessario verificar
%que P(A\B)=P(A) e que P(B\A)=P(B)
%utilizando a formula da condicional verificamos que ambos os
%acontecimentos ocorrem, logo sao independentes

% tambem se verifica que P(AnB)=P(A)*P(B) -> independentes

%% d)

%usando a logica de cima, P(C)=1/3 e P(D)= 25/36, calculamos tambem P(CnD)
%e verificamos que esta e diferente a multiplicaçao das probabilidades de c
%e d, logo nao sao acontecimentos independentes

